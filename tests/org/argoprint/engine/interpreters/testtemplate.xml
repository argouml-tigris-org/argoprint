<root>
    <!-- Test tag for default. -->
    <tok>
        Tjohoj!!
    </tok>

    <!-- Test tag for Call. -->
    <ap:call what="getString()"/>
    <expected_ap_call_result>Return value of getString() in DataSourceStub.</expected_ap_call_result>

    <!-- Test tag for Iterate. -->
    <ap:iterate what="getCollection()" iterator="it">
        <ap:call what="getString()"/>
    </ap:iterate>
    <expected_ap_iterate_result>
        Return value of getString() in DataSourceStub.Return value of getString() in DataSourceStub.Return value of getString() in DataSourceStub.Return value of getString() in DataSourceStub.Return value of getString() in DataSourceStub.
    </expected_ap_iterate_result>

    <!-- Test tag for If. -->
    <ap:if cond="false">
        <ap:then>
            Then.
        </ap:then>
        <ap:else>
            Else.
        </ap:else>
    </ap:if>

    <!-- Test tag for Bind. -->
    <ap:bind what="img">
        <ap:attr name="src">Hoj</ap:attr>
    </ap:bind>
</root>
